# Copy to .env for local runs. .env is gitignored. Never commit real values.
API_KEY=change-me-local-only
UPSTREAM_BASE=https://api.example.com
REQUEST_TIMEOUT=20
LOG_LEVEL=INFO
# Leave SENTRY_DSN blank locally so local runs do not report errors.
SENTRY_DSN=
